{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Build",
            "type": "shell",
            "command": "docker-tasmota/entrypoint.sh -e tasmota",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
        {
            "label": "Run Build - Minimal",
            "type": "shell",
            "command": "docker-tasmota/entrypoint.sh -e tasmota-minimal",
            "options": {
            "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
        
    ]
}